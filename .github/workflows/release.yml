# ClearCutt Hardened Fleets Unified Release Workflow

name: ClearCutt Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      custom_version:
        description: "Custom version tag to release (e.g. v1.0.0). If omitted, automatically calculated via conventional commits."
        required: false
        default: ""

permissions:
  contents: read
  actions: read

# Releases mutate GHCR tags + SLSA provenance. The release identity is pinned to
# refs/heads/main in clearcutt.yaml and admission examples, so dispatches
# from other refs are rejected before any mutable release work starts.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  check-version:
    name: Check Version Bump
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version-calc.outputs.version }}
      version_type: ${{ steps.version-calc.outputs.version_type }}
    steps:
      - name: Enforce main release identity
        if: ${{ github.ref != 'refs/heads/main' }}
        run: |
          echo "::error::ClearCutt release identity is pinned to refs/heads/main. Dispatch releases from main, or update fleet config, policies, and verifier commands together."
          exit 1

      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Calculate Next Version
        id: versioning
        uses: paulhatch/semantic-version@9f72830310d5ed81233b641ee59253644cd8a8fc # v6.0.2
        with:
          tag_prefix: "v"
          major_pattern: "BREAKING CHANGE"
          minor_pattern: "feat"
          version_format: "${major}.${minor}.${patch}"

      - name: Resolve Version Output
        id: version-calc
        env:
          # Hoisted out of the run script so the GitHub templater never
          # expands untrusted context into shell code.
          CUSTOM_INPUT: ${{ github.event.inputs.custom_version }}
          CALC_VERSION: ${{ steps.versioning.outputs.version }}
          CALC_VERSION_TYPE: ${{ steps.versioning.outputs.version_type }}
        run: |
          if [ -n "$CUSTOM_INPUT" ]; then
            RAW_VERSION="${CUSTOM_INPUT#v}"
            echo "version_type=custom" >> "$GITHUB_OUTPUT"
          else
            RAW_VERSION="$CALC_VERSION"
            echo "version_type=$CALC_VERSION_TYPE" >> "$GITHUB_OUTPUT"
          fi
          echo "version=$RAW_VERSION" >> "$GITHUB_OUTPUT"

  release-gate:
    name: Staging Gate (Approve to Release v${{ needs.check-version.outputs.version }})
    needs: [check-version]
    if: ${{ github.event_name == 'workflow_dispatch' || needs.check-version.outputs.version_type != 'none' }}
    runs-on: ubuntu-latest
    environment: production # <-- Manual approval gate here!
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Create and Push Git Tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Hoisted out of the run script so the GitHub templater never
          # expands untrusted context into shell code.
          RAW_VERSION: ${{ needs.check-version.outputs.version }}
        run: |
          set -euo pipefail
          
          VERSION_TAG="v$RAW_VERSION"
          
          echo "Release approved! Tagging commit and pushing $VERSION_TAG..."

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git rev-parse "$VERSION_TAG" >/dev/null 2>&1; then
            echo "Tag $VERSION_TAG already exists in repository."
          else
            git tag "$VERSION_TAG"
            git push origin "$VERSION_TAG"
          fi

  build-cli:
    name: Build & Sign Go CLI Binaries
    needs: [check-version, release-gate]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: v${{ needs.check-version.outputs.version }}

      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: 'cli/go.mod'
          cache-dependency-path: 'cli/go.sum'
          cache: true

      - name: Install Sigstore Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: 'v3.0.6'

      # Built with plain go build rather than a clearcutt subcommand. Releasing
      # a tool should not depend on that tool: a CLI that cannot build breaks
      # its own release, and the subcommand that used to do this lived in the
      # image-factory command group that no longer exists.
      - name: Build, Sign, and Checksum CLI Assets
        env:
          VERSION: v${{ needs.check-version.outputs.version }}
        run: |
          set -euo pipefail
          mkdir -p build-outputs
          for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64; do
            os="${target%/*}"; arch="${target#*/}"
            out="build-outputs/clearcutt-${os}-${arch}"
            CGO_ENABLED=0 GOOS="$os" GOARCH="$arch" go -C cli build \
              -trimpath \
              -ldflags "-s -w -buildid= -X github.com/northcutted/clearcutt/internal/commands.Version=${VERSION}" \
              -o "../${out}" ./cmd/clearcutt
            cosign sign-blob --yes "$out" \
              --output-signature "${out}.sig" \
              --output-certificate "${out}.pem"
          done
          ( cd build-outputs && sha256sum clearcutt-* > SHA256SUMS.txt )

      - name: Upload CLI Artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-cli-binaries
          path: |
            build-outputs/clearcutt-*
            build-outputs/SHA256SUMS.txt
          retention-days: 7

  finalize-release:
    name: Finalize GitHub Release Assets
    needs: [check-version, release-gate, build-cli]
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: v${{ needs.check-version.outputs.version }}

      - name: Download all release assets
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: build-outputs/
          pattern: release-*
          merge-multiple: true

      # Uses gh directly for the same reason the build step does: the release of
      # a tool must not depend on installing that tool first.
      - name: Finalize GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: v${{ needs.check-version.outputs.version }}
        run: |
          set -euo pipefail
          if gh release view "$VERSION" >/dev/null 2>&1; then
            gh release upload "$VERSION" build-outputs/* --clobber
          else
            gh release create "$VERSION" build-outputs/* \
              --title "$VERSION" --generate-notes
          fi

  deploy-site:
    name: Deploy Catalog Site
    needs: finalize-release
    permissions:
      contents: read
      packages: read
      pages: write
      id-token: write
    uses: ./.github/workflows/publish-pages.yml
    secrets: inherit
