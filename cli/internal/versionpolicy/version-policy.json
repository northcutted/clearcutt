{
  "$comment": "Canonical version + lifecycle policy for ClearCutt runtime lines. EDIT THIS FILE (core/lib/version-policy.json); cli/internal/versionpolicy/version-policy.json is a byte-identical mirror embedded in the CLI binary and enforced by TestVersionPolicyMirrorMatchesCanonical. Channels: lts/current are production-eligible (status=active); preview is opt-in (status=preview, only via --allow-preview); experimental marks toolchain lines (status=experimental, support=unsupported). omitInProduction marks toolchain languages whose runtime is not shipped in production tiers (mirrors registry.nix) and are therefore never productionAllowed. This is the single source of truth for runtime-line classification. Every language listed here must have a matching recipe in core/lib/registry.nix.",
  "schemaVersion": 1,
  "languages": {
    "java": {
      "versions": {
        "21": "lts",
        "25": "lts"
      }
    },
    "node": {
      "versions": {
        "22": "lts",
        "24": "lts",
        "26": "preview"
      }
    },
    "python": {
      "versions": {
        "3.13": "current",
        "3.14": "lts"
      }
    },
    "go": {
      "omitInProduction": true,
      "versions": {
        "1.25": "experimental",
        "1.26": "lts",
        "1.27": "current"
      }
    }
  }
}
