---
interface Props {
  kind: 'ok' | 'warn' | 'danger' | 'info';
  label: string;
}
const { kind, label } = Astro.props;
const cls = {
  ok: 'chip-accent',
  warn: 'chip-warn',
  danger: 'chip-danger',
  info: 'chip',
}[kind];
---
<span class={cls}>{label}</span>
