{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "clearcutt.catalog.package/v1",
  "title": "PackageEntry",
  "description": "A single software package discovered in an image SBOM. This is the standalone, referenceable form of the PackageEntry definition embedded in image-record.v1.schema.json.",
  "type": "object",
  "required": ["name", "version", "cpes", "license", "supplier", "spdxId"],
  "properties": {
    "name": { "type": "string" },
    "version": { "type": "string" },
    "purl": { "type": ["string", "null"] },
    "cpes": {
      "type": "array",
      "items": { "type": "string" }
    },
    "license": { "type": "string" },
    "supplier": { "type": "string" },
    "nixStorePath": { "type": ["string", "null"] },
    "spdxId": { "type": "string" },
    "layerDigest": {
      "type": ["string", "null"],
      "description": "Digest of the layer that introduced this package. Null when package-to-layer mapping is unavailable, which disables the per-layer package and CVE views."
    }
  }
}
