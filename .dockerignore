# The build needs Go sources and the embedded assets the CLI generates from
# site/ and the repo tree. Everything else is noise that busts the build cache.
.git
site/node_modules
site/dist
core/build-outputs
**/*.tar.gz
dist
